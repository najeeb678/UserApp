import { Box } from "@mui/system";
import React from "react";

const Home = () => {
  return (
    <Box
      sx={{
        position: "absolute",
        top: "50%",
        left: "50%",
        transform: "translate(-50%, -50%)",
      }}
    >
      <h1>home</h1>
    </Box>
  );
};

export default Home;
